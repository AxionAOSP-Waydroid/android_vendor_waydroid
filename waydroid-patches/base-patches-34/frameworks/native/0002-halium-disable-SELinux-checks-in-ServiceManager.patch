From 6c10e8b4cc58e5a734005323af312c33d2345715 Mon Sep 17 00:00:00 2001
From: SupeChicken666 <me@supechicken666.dev>
Date: Fri, 18 Jul 2025 12:44:08 +0000
Subject: [PATCH 02/13] (halium) disable SELinux checks in ServiceManager

Change-Id: Ida8e91453db58b1123468931c94d4a40d273b0b3
Signed-off-by: SupeChicken666 <me@supechicken666.dev>
---
 cmds/servicemanager/Access.cpp | 11 +++++++++--
 1 file changed, 9 insertions(+), 2 deletions(-)

diff --git a/cmds/servicemanager/Access.cpp b/cmds/servicemanager/Access.cpp
index 711038ce63..0df069a798 100644
--- a/cmds/servicemanager/Access.cpp
+++ b/cmds/servicemanager/Access.cpp
@@ -32,6 +32,9 @@ constexpr bool kIsVendor = false;
 
 #ifdef __ANDROID__
 static std::string getPidcon(pid_t pid) {
+    // Disabled for Halium
+    return "";
+
     android_errorWriteLog(0x534e4554, "121035042");
 
     char* lookup = nullptr;
@@ -90,9 +93,10 @@ Access::Access() {
     cb.func_log = kIsVendor ? selinux_vendor_log_callback : selinux_log_callback;
     selinux_set_callback(SELINUX_CB_LOG, cb);
 
-    CHECK(selinux_status_open(true /*fallback*/) >= 0);
+    // Disabled for Halium
+    /*CHECK(selinux_status_open(true) >= 0);
 
-    CHECK(getcon(&mThisProcessContext) == 0);
+    CHECK(getcon(&mThisProcessContext) == 0);*/
 #endif
 }
 
@@ -131,6 +135,9 @@ bool Access::canList(const CallingContext& ctx) {
 
 bool Access::actionAllowed(const CallingContext& sctx, const char* tctx, const char* perm,
         const std::string& tname) {
+    // Disabled for Halium
+    return true;
+
 #ifdef __ANDROID__
     const char* tclass = "service_manager";
 
-- 
2.50.1

